<script>
    const deviceID = <%= deviceID %>
    const changeModeIdle = () => {
        $.post('/api/dashboard/' + deviceID + '/status', {mode: 'idle'});
    }
    const changeModeIdle = () => {
        $.post('/api/dashboard/' + deviceID + '/status', {mode: 'idle'});
    }
    const changeModeIdle = () => {
        $.post('/api/dashboard/' + deviceID + '/status', {mode: 'idle'});
    }
    
</script>